import { FormatNamberPipe } from './format-namber-pipe';

describe('FormatNamberPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNamberPipe();
    expect(pipe).toBeTruthy();
  });
});
